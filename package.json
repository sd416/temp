{
  "name": "temp",
  "version": "1.0.0",
  "description": "Cloudflare Worker that generates a dynamic SVG bar chart for website traffic",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.3",
    "@cloudflare/workers-types": "^4.20260412.2",
    "typescript": "^6.0.2",
    "vitest": "~4.1.4",
    "esbuild": "^0.28.0",
    "wrangler": "^4.72.0"
  }
}
